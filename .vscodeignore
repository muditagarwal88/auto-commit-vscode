# Source — only ship the compiled bundle
src/
tsconfig.json
esbuild.js
.eslintrc*

# Dev dependencies are bundled by esbuild, so node_modules is not needed
node_modules/

# Misc
.vscode/
.claude/
.gitignore
Untitled
*.map
